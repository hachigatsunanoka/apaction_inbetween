version: "1.0"

action:
  name: "Nuke: New Scene..."

  version: 1
  id: "inbetween::nuke::new"
  category: "inbetween/dcc/nuke"
  enable: false
  type: python
  author: "inbetween"
  icon:
    path: "../resource/nuke_icon.svg"

  script: "../script/nuke_new.py"

  register:
    new_file:
      enable: true
      filter: "*/nuke"