version: "1.0"

action:
  name: "Folder: Worker Initial"

  version: 1
  id: "inbetween::pipeline::worker"
  category: "inbetween/pipeline"
  enable: false
  type: python
  author: "inbetween"
  icon:
    path: :/icons/user-interface/folder.svg

  script: "code/worker.py"

  register:
    new_folder:
      enable: true