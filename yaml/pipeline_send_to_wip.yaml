version: "1.0"

action:
  name: "Pipeline: Send to WIP..."

  version: 1
  id: "inbetween::pipeline::send_to_wip"
  category: "inbetween/pipeline"
  enable: false
  type: python
  author: "inbetween"
  icon:
    path: "../resource/ib_white.svg"

  script: "../script/pipeline_send_to_wip.py"

  register:
    file:
      enable: true