version: "1.0"

action:
  #Must Have Properties
  name: "Assign file as preview ..."

  #Optional Properties
  version: 1
  id: "inbetween::pipeline::assign_as_preview"
  category: "inbetween/pipeline"
  enable: false
  type: python
  author: "inbetween"
  icon:
    path: "resource/houdini_icon.svg"

  script: "code/assign_as_preview.py"
  # settings: code/launchr_settings.py

  #Where to register this action
  register:
    file:
      filter: "*.mp4; *.png; *.mov; *.jpg"