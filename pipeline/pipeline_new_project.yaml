version: "1.0"
action:
  name: "Create New Project..."

  version: 1
  id: "inbetween::pipeline::new_project"
  category: "inbetween/pipeline"
  enable: true
  type: python
  author: "inbetween"
  description: Create a new project with a folder structure and metadata.
  icon:
    path: "resource/ib.svg"

  script: "code/new_project.py"

  register:
    workspace_overview:
      enable: true
    
    